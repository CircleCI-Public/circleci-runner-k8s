apiVersion: v1
kind: Service
metadata:
  name: {{ include "circleci-runner.fullname" . }}
  labels:
    {{- include "circleci-runner.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "circleci-runner.selectorLabels" . | nindent 4 }}
